namespace DataManager
{
    /// <summary>
    /// Enum marks the game result status for the player that can be possible for different scenarios. 
    /// </summary>
    public enum GameResultStatus
    {
        WON,
        LOSS,
        LOSS_BY_TIMEOUT,
        TIE,
    }
}